services:
  - type: web
    name: urban-sound-narrative  # Or your preferred name
    env: docker                 # Key change: Use Docker
    dockerfilePath: ./Dockerfile  # Points to it
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.7
    autoDeploy: true